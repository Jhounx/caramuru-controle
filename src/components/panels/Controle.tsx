import React from 'react'
import {Flex, Button} from '@chakra-ui/react'

const Controle: React.FunctionComponent = () => {
  return (
    <Flex>
      <Button>
        Iniciar calibração
      </Button>
    </Flex>
  )
}

export default Controle